{
  "name": "@taisiusyut/common",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "build": "rm -rf ./*.tsbuildinfo && tsc -p tsconfig.json",
    "postinstall": "yarn build"
  },
  "dependencies": {
    "@taisiusyut/server": "1.0.0",
    "axios": "^0.21.1",
    "typescript": "^4.2.3"
  }
}
